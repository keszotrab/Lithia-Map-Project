# LithiaMapProject 
A simple project for a map/wiki app based on my game's world.

# WORK IN PROGRESS

#Todo:
-fix window referenceError
-fix the rendering when scrolled too much
-fix scrolling zoom to zoom towards the mouse cursor placement. 
-add Tile Cache, lazy loading
-place map into a nicer component/divs
-add UI, window border, little div/box for info about locations. 
-add Nodes on map, that when clicked load the info about the node. 
-Beautify the info window a bit. Animation with a scroll opening/closing when tapped on top of the window or node. 

-Make a separete info/wiki site.
-Add a relation graph to the site with nodes connected between each other showing relations between them, based on chosen filter